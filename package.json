{
	"name": "pushover-cloudflare-build-notifications",
	"description": "Get Cloudflare Workers build notifications on your phone via Pushover.",
	"type": "module",
	"private": true,
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"tail": "wrangler tail",
		"test": "vitest run",
		"test:watch": "vitest",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/vitest-pool-workers": "0.16.3",
		"@emnapi/core": "1.10.0",
		"@emnapi/runtime": "1.10.0",
		"typescript": "6.0.3",
		"vitest": "4.1.5",
		"wrangler": "4.90.0"
	}
}
